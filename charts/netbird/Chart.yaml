apiVersion: v2
name: netbird
description: A Helm chart to deploy self-hosted NetBird
type: application
version: 0.1.0
appVersion: "0.77.0"
dependencies:
  - name: postgresql
    version: "16.4.6"
    repository: "https://charts.bitnami.com/bitnami"
    condition: postgresql.enabled
