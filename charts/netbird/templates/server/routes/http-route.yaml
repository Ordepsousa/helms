{{- if and .Values.gateway.enabled (not .Values.ingress.enabled) -}}
{{- if .Capabilities.APIVersions.Has "gateway.networking.k8s.io/v1/HTTPRoute" -}}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
    name: {{ include "netbird.fullname" . }}-server
    labels:
        {{- include "netbird.labels" . | nindent 8 }}
spec:
    {{- with .Values.gateway.parentRefs }}
    parentRefs:
        {{- toYaml . | nindent 8 }}
    {{- end }}
    hostnames:
        - {{ .Values.domain | quote }}
    rules:
        -   backendRefs:
                -   group: ''
                    kind: Service
                    name: {{ include "netbird.serverServiceName" . }}
                    port: {{ .Values.server.services.http.port }}
            matches:
                -   path:
                        type: PathPrefix
                        value: /api
                -   path:
                        type: PathPrefix
                        value: /oauth2
        -   backendRefs:
                -   group: ''
                    kind: Service
                    name: {{ include "netbird.serverRelayServiceName" . }}
                    port: {{ .Values.server.services.http.port }}
                    weight: 1
            matches:
                -   path:
                        type: PathPrefix
                        value: /ws-proxy
                -   path:
                        type: PathPrefix
                        value: /relay
            timeouts:
                request: 3600s
                backendRequest: 3600s
{{- end }}
{{- end }}